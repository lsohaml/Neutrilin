import React, { useState } from 'react';
import { api } from '../api';

export default function Login({ onLogin }) {
  const [mode, setMode] = useState('login'); const [email, setEmail] = useState('demo@neutrilin.local'); const [password, setPassword] = useState('DemoPassword123'); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  async function submit(e) { e.preventDefault(); setBusy(true); setError(''); try { const r = await api(`/auth/${mode === 'login' ? 'login' : 'signup'}`, {method:'POST', body:JSON.stringify({email,password})}); localStorage.setItem('neutrilin_token',r.token); localStorage.setItem('neutrilin_user',JSON.stringify(r.user)); onLogin(r.user); } catch(err) { setError(err.message); } finally { setBusy(false); } }
  return <div className="min-h-screen bg-slate-50 grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md bg-white rounded-3xl p-8 shadow-sm border border-slate-100"><h1 className="text-3xl font-black text-emerald-700 italic">NutriGuide</h1><p className="text-slate-500 mt-2 mb-8">{mode==='login'?'Sign in to your demo account.':'Create a demo account.'}</p><label className="block text-sm font-semibold mb-2">Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="w-full p-3 rounded-xl bg-slate-50 mb-5 outline-none focus:ring-2 focus:ring-emerald-500"/><label className="block text-sm font-semibold mb-2">Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} className="w-full p-3 rounded-xl bg-slate-50 mb-5 outline-none focus:ring-2 focus:ring-emerald-500"/>{error&&<div className="mb-5 p-3 rounded-xl bg-red-50 text-red-700 text-sm">{error}</div>}<button disabled={busy} className="w-full bg-emerald-600 text-white p-3 rounded-xl font-bold disabled:opacity-50">{busy?'Working…':mode==='login'?'Sign in':'Create account'}</button><button type="button" onClick={()=>setMode(mode==='login'?'signup':'login')} className="w-full mt-4 text-sm text-emerald-700">{mode==='login'?'Need an account? Create one':'Already have an account? Sign in'}</button></form></div>
}
