import React, { useState } from 'react';
import { Plus } from 'lucide-react';
import { api } from '../api';
export default function AddFoodForm({onAdded}) { const [name,setName]=useState(''); const [calories,setCalories]=useState(''); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); async function add(){ if(!name.trim()||calories==='')return; setBusy(true);setError(''); try{const r=await api('/logs',{method:'POST',body:JSON.stringify({name,calories:Number(calories)})});setName('');setCalories('');onAdded?.(r);}catch(e){setError(e.message)}finally{setBusy(false)}} return <div className="bg-white p-6 rounded-3xl border border-slate-100 shadow-sm"><h3 className="text-xl font-bold text-slate-800 mb-5">Quick Log Meal</h3><div className="flex flex-col md:flex-row gap-3"><input value={name} onChange={e=>setName(e.target.value)} placeholder="What did you eat?" className="flex-1 p-4 bg-slate-50 rounded-2xl outline-none focus:ring-2 focus:ring-emerald-500"/><input value={calories} onChange={e=>setCalories(e.target.value)} type="number" min="0" placeholder="Calories" className="w-full md:w-36 p-4 bg-slate-50 rounded-2xl outline-none focus:ring-2 focus:ring-emerald-500"/><button disabled={busy} onClick={add} className="bg-emerald-600 text-white px-6 py-4 rounded-2xl font-bold flex items-center justify-center gap-2 disabled:opacity-50"><Plus size={20}/>{busy?'Saving…':'Add to Log'}</button></div>{error&&<p className="text-sm text-red-600 mt-3">{error}</p>}</div> }
